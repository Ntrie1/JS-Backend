
        <section id="viewhotelDetails">
            <h2>Details</h2>
            <div class="hotel-ticket">
                <div class="hotel-left">
                    <img src="{{hotel.imageUrl}}"
                        alt="">
                </div>
                <div class="hotel-right">
                    <div>
                        <h3>{{hotel.name}}</h3>
                    </div>
                    <div>
                        {{hotel.city}}
                    </div>
                    <p><span >Free rooms: {{hotel.freeRooms}}</span> </p>
                  
                  {{#if hasBooked}}
                    <p><span class="green">You already have booked a room</span> </p>
                    {{/if}}

               
                    {{#if isOwner}}
                    <a href="/hotels/{{hotel._id}}/edit" class="edit">Edit</a>
                    <a href="/hotels/{{hotel._id}}/delete" class="remove">Delete</a>
                    {{else}}
                    {{#unless hasBooked}}
                         <a href="/hotels/{{hotel._id}}/book" class="book">Book</a>
                         {{/unless}}
                    {{/if}}
                </div>

            </div>
        </section>

